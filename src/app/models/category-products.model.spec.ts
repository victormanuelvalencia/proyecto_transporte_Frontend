import { CategoryProducts } from './category-products.model';

describe('CategoryProducts', () => {
  it('should create an instance', () => {
    expect(new CategoryProducts()).toBeTruthy();
  });
});
